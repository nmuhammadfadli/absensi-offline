import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ────────────────────────────────────────────────────────────────
  static const primary       = Color(0xFF1565C0); // blue-800
  static const primaryLight  = Color(0xFF1E88E5); // blue-600
  static const primaryDark   = Color(0xFF0D47A1); // blue-900
  static const onPrimary     = Colors.white;

  // ── Status Absensi ───────────────────────────────────────────────────────
  static const hadir       = Color(0xFF2E7D32); // green-800
  static const hadirBg     = Color(0xFFE8F5E9); // green-50
  static const izin        = Color(0xFF1565C0); // blue-800
  static const izinBg      = Color(0xFFE3F2FD); // blue-50
  static const sakit       = Color(0xFFE65100); // orange-900
  static const sakitBg     = Color(0xFFFFF3E0); // orange-50
  static const alfa        = Color(0xFFC62828); // red-800
  static const alfaBg      = Color(0xFFFFEBEE); // red-50

  // ── Neutral ──────────────────────────────────────────────────────────────
  static const textPrimary   = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textHint      = Color(0xFFBDBDBD);
  static const divider       = Color(0xFFEEEEEE);
  static const cardBg        = Colors.white;
  static const scaffoldBg    = Color(0xFFF5F6FA);

  // ── Gender ───────────────────────────────────────────────────────────────
  static const lakiLaki    = Color(0xFF1565C0);
  static const lakiLakiBg  = Color(0xFFE3F2FD);
  static const perempuan   = Color(0xFFAD1457);
  static const perempuanBg = Color(0xFFFCE4EC);
}